//Homework for lesson 1, #8
int count = 0;
List<int> even_nums = new List<int>();
Console.WriteLine("Enter the number");
int num = int.Parse(Console.ReadLine());
for (int index = 1; index <= num; index++)
    if (index % 2 == 0)
        {
        Console.WriteLine(index);
        }
    else {

    }